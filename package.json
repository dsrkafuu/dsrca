{
  "private": true,
  "name": "dsrca",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "turbo run build --filter=./packages/*",
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.6.2"
  }
}
